# Automate Incident Response and Detection
You are given an environment with guardduty configured to detect some sort of incident and take snapshot of the instance then trigger a lambda function which is supposed to isolate that ec2 instance with a restrictive security group.

The lambda is buggy so your task now is to debug that lambda function.
