# Too Many Secrets (Day 3)
You are given an EC2 instance with no ssh keys (you cannot ssh directly) and you must find a secret which is stored in the source code inside that EC2 instance.

Your task is to find a way to access that ec2 instance without ssh keys and find that secret. (Through ssm, or through instance connect, etc.)
